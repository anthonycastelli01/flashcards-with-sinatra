<h2><%= @deck.name %></h2>
<form method="post" action="/users/<%= @user.id %>/decks/<%= @deck.id %>/cards/create">
  <h3>Define New Card:</h3>
  <label for="term">Card Name: </label>
  <input type="text" name="term">

  <label for="definition">Definition: </label>
  <input type="text" name="definition">

  <button type="submit">Create Card</button>
</form>


<% @deck.cards.each do |card| %>

<% end %>

<h2 class="sub-header">Cards</h2>
<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Term</th>
        <th>Definition</th>
        <th>Times Correct</th>
      </tr>
    </thead>
    <tbody>
      <% @deck.cards.each do |card| %>
      <tr>
        <td><%= card.id %></td>
        <td><%= card.term %></td>
        <td><%= card.definition %></td>
        <td><%=  %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>